# HyperFleet Adapter Deployment Configuration for testing
adapter:
  name: test-adapter
  version: "0.1.0"

clients:
  hyperfleet_api:
    timeout: 2s
    retry_attempts: 3
    retry_backoff: exponential

  kubernetes:
    api_version: "v1"
