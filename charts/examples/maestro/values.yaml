adapterConfig:
  create: true
  files:
    adapter-config.yaml: examples/maestro/adapter-config.yaml

adapterTaskConfig:
  create: true
  files:
    task-config.yaml: examples/maestro/adapter-task-config.yaml
    manifestwork.yaml: examples/maestro/adapter-task-resource-manifestwork.yaml

broker:
  create: true
  googlepubsub:
    project_id: CHANGE_ME
    subscription_id: CHANGE_ME
    topic: CHANGE_ME
    dead_letter_topic: CHANGE_ME

image:
  registry: CHANGE_ME
  repository: hyperfleet-adapter
  pullPolicy: Always
  tag: latest

env:
  - name: NAMESPACE
    valueFrom:
      fieldRef:
        fieldPath: metadata.namespace
  - name: SERVICE_ACCOUNT
    valueFrom:
      fieldRef:
        fieldPath: spec.serviceAccountName
  - name: SIMULATE_RESULT
    value: success     # other possible values: success, failure, hang, crash, invalid-json, missing-status

rbac:
   resources:
     - namespaces
     - serviceaccounts
     - configmaps
     - deployments
     - roles
     - rolebindings
     - jobs
     - jobs/status
     - pods
